<h1>Bookmarks#new</h1>
<p>Find me in app/views/bookmarks/new.html.erb</p>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-8 col-12 p-3 rounded border border-1 border-dark-grey ">
    <%= simple_form_for [@list, @bookmark] do |f| %>
      <%= f.input :comment, class: '', as: :text %>
      <%= f.association :movie , collection: Movie.all.order(:title) %>
      <%= f.button :submit, class: 'mt-3 btn btn-primary' %>
    <% end %>
    </div>
  </div>
</div>
